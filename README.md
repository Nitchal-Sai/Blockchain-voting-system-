# Blockchain-voting-system-
its about a voting system using blockchain technology in here u will see how a voting system performs(its just an example and this doesnt mean that every voting system looks like this )
